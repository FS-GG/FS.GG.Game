# FS.GG.Game 0.14.0 release preparation

Issue: FS-GG/FS.GG.Game#606
SDD work: `606-game-0-14-0-coherent-release`
Published baseline: `v0.13.0` at `a2eef0cb54d12cd9b81b426d76de275293142e13`

## Release decision

This is a stable MINOR cut. The repository remains one coherent runtime set:

- `FS.GG.Game.Core` 0.14.0
- `FS.GG.Game.Render` 0.14.0
- `FS.GG.Game.Harness` 0.14.0

The public delta from 0.13.0 is additive: `src/Game.Harness/Journey.fsi` adds 66 lines and removes none,
including `JourneyReceipt.definitionDigest`, `ActionCoverageReport`, and
`Journey.checkActionCoverage`. Across the three package-owned source trees, the 58 commits since the
0.13.0 source add 199 lines and remove one; SDK Package Validation against the published 0.13.0 packages
passes during the coherent pack.

## Candidate verification

The tracked composite JUnit receipt at
`readiness/606-game-0-14-0-coherent-release/test-results/game-0-14-release.junit.xml` records ten
passing checks:

- Game.Core.Tests: 730 passed
- Game.Render.Tests: 23 passed
- Game.Harness.Tests: 97 passed
- Playtest.Cli.Tests: 28 passed
- a force-evaluated restore records all eight internal project-reference ranges at 0.14.0
- the release workflow positive control (both actual push operations are accepted)
- three independent mutation witnesses (replacing only the org push, only the public push, or both
  push operations is rejected)
- the version mutation control (a 0.14.1 scalar is rejected)
- the exact three-package inventory, nuspec repository commit binding, and clean Journey API consumer

`readiness/fsharp-public-surface.json` is the Governance 1.12.1 producer receipt for the changed Harness
surface. It is applicable, reads 12 compiled signature modules, is not malformed, and reports the
repository's current `warn` maturity. SDD verification reports 30/30 findings ready, all 15 evidence
obligations observed, and ship readiness green.

## Post-merge release obligations

The implementation PR prepares release source only. After independent review, host acceptance, and
merge, the release operator must:

1. Run the full gates at the merged SHA and create immutable tag `v0.14.0` there.
2. Let the producer workflow prepare one coherent artifact set and publish those bytes to GitHub
   Packages first, then nuget.org without re-packing.
3. Download all three packages from both feeds, compare payloads excluding only feed-added signatures,
   and verify each nuspec repository commit resolves to the tag SHA.
4. Restore a clean consumer from the public feed and compile the three Journey APIs named above.
5. Reconcile the central registry and only then dispatch evidence-backed consumer updates.

Existing exact 0.13.0 consumer pins remain valid; this release does not require a source migration.
